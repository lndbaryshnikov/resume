mixin block-header(blockName)
  header.block-header
    img.block-header__icon(
      src=require(`../../components/block-header/icons/${blockName}-icon.png`),
      alt=blockName
    )
    .block-header__name= blockName
    .block-header__line
